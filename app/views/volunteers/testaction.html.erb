<h1>Volunteers#testaction</h1>
<p>Find me in app/views/volunteers/testaction.html.erb</p>

<%= simple_form_for :walkin, { url: walkin_path } do |f| %>
    #walkin represents a process not related to a model, need to set up path so that it calls testaction and populates params
    <%= f.input :name_first %>
    <%= f.input :name_last %>
    <%= f.input :email %>
    <%= f.button :submit, "Add to database" %>
<% end %>
